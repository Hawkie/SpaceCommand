
Integrate Karma and Jasmine for browser testing
Change models/actors so they are more versitile and work with react 
add redux, reduce and react
remove sound that doen't seem to work on a phone safari browser?

fixes:

The bullet removal when player hits player

the space explosion dispersal - so its evenly spread - not upwards as in ground explosion.

put all helper static classes in one place - functions

condense all the field generators

remove controllers  - use model state to determine key presses and inputs and then create Tester Actors to apply state to model changes

change singlegame object and multi game object to have functions to get model from parent state.
implement keep in as an actor
remove model from game object
add sound to game object